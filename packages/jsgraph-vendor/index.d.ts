// Ambient type declarations for the wabis framework globals.
//
// PHASE 1 (pending): declare here (or in ./types/*.d.ts) the global functions and
// objects the apps/adapters actually use, e.g.:
//
//   declare global {
//     function NewGraphX3D(params: unknown): JsGraphX3D;
//     const JsgVect3: { /* ... */ };
//   }
//
// Inventory the real surface first with grep over apps/ before declaring.
export {};
